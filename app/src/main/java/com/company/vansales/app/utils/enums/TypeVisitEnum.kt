package com.company.vansales.app.utils.enums

import com.company.vansales.app.utils.DocumentsConstants


enum class TypeVisitEnum(var value: String) {
    BILL_TO_BILL(DocumentsConstants.Bill_to_Bill),
    APPROVAL(DocumentsConstants.APPROVAL)
}