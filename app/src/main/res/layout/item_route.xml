<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/route_item"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginStart="6dp"
    android:layout_marginTop="2dp"
    android:layoutDirection="locale"
    android:layout_marginEnd="6dp"
    android:background="@drawable/rectangle_white"
    android:clickable="true"
    android:focusable="true">


    <TextView
        android:id="@+id/item_route_textView"
        android:layout_width="match_parent"
        android:layout_height="32dp"
        android:layout_marginStart="12dp"
        android:layout_marginTop="16dp"
        android:layout_marginEnd="12dp"
        android:layout_marginBottom="16dp"
        android:drawableEnd="@drawable/ic_road"
        android:text="Route Number one"
        android:textAlignment="center"
        android:textColor="@color/dark_text"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <androidx.appcompat.widget.AppCompatCheckBox
        android:id="@+id/item_route_imageView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:button="@drawable/checkbox"
        android:visibility="visible"
        android:checked="false"
        app:layout_constraintBottom_toBottomOf="@+id/item_route_textView"
        app:layout_constraintStart_toStartOf="@+id/item_route_textView"
        app:layout_constraintTop_toTopOf="@+id/item_route_textView" />

</androidx.constraintlayout.widget.ConstraintLayout>